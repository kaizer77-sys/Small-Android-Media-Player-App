package com.example.smallandroidmediaplayerapp.presentation.service

import com.example.smallandroidmediaplayerapp.domain.model.AudioFile

object PlayerAudioManager {
    var audioFiles: List<AudioFile> = emptyList()
}